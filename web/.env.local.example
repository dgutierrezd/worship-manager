# Public API base URL — defaults to the production Vercel deployment.
# Backend proxy — Next.js rewrites /api/* to this URL.
# In dev: point directly at the Express server.
BACKEND_INTERNAL_URL=http://localhost:3000
